export default interface PlaneSettingsInterface {
    speed: number;
    directionIndex: number,
    rotationDegrees: number,
    width: number,
    height: number,
    scale: number,
    x: number,
    y: number,
}
