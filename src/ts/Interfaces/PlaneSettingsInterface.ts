export default interface PlaneSettingsInterface {
    speed: number;
    health: number,
    directionIndex: number,
    directionDegrees: number,
    width: number,
    height: number,
    scale: number,
    x: number,
    y: number,
    keyFire: string,
    keyLeft: string,
    keyRight: string
}
